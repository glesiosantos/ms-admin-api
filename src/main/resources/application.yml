server:
  port: 5000
  servlet:
    context-path: /api
spring:
  profiles:
    active: prod
  application:
    name: ms-admin-api

# swagger-ui custom path
springdoc:
  swagger-ui:
    path: /swagger-ui.html

rabbitmq:
  exchanges:
    criar-oficinas: criar-oficinas.ex